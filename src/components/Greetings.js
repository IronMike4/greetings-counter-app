import React from "react";

// The Greetings component
const Greetings = () => {
  // Render a paragraph with the text "Hello, World!"
  return <p>Hello, World!</p>;
};

// Export the Greetings component as the default export
export default Greetings;
